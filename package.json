{
  "name": "hand-sign-detection-frontend",
  "version": "1.0.0",
  "description": "Simple frontend for Hand Sign Detection API",
  "main": "index.html",
  "scripts": {
    "start": "npx serve -l 3000 .",
    "dev": "npx serve -l 3000 .",
    "build": "echo 'Static HTML - no build required'",
    "lint": "echo 'No linting configured for vanilla JS'"
  },
  "keywords": [
    "hand-sign-detection",
    "frontend",
    "webcam"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "serve": "^14.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "browserslist": [
    ">0.5%",
    "not dead"
  ]
}
